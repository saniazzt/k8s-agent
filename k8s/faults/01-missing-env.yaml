# FAULT (easy): required env var disappears (ConfigMap key deleted).
# Symptom: pods CrashLoopBackOff, log "FATAL missing required env: ['DB_HOST']"
spec:
  template:
    spec:
      containers:
        - name: app
          envFrom:
            - configMapRef:
                name: demo-app-config
